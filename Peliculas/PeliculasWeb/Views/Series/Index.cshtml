@model PeliculasWeb.Models.HomeViewModel

<style>
    body {
        background-color: #111; /* Fondo general oscuro */
        color: #FFD700; /* Texto dorado */
    }

    .form-select {
        background-color: #000;
        color: #FFD700;
        border: 1px solid #FFD700;
    }

    .card {
        background-color: #000;
        border: 1px solid #FFD700;
        color: #FFD700;
    }

    .btn-warning {
        background-color: #FFD700;
        color: #000;
        border: none;
    }

        .btn-warning:hover {
            background-color: #e6c200;
        }

    .btn-outline-success,
    .btn-success {
        border-color: #FFD700;
        color: #FFD700;
        background-color: transparent;
    }

        .btn-outline-success:hover {
            background-color: #FFD700;
            color: #000;
        }

        .btn-success:disabled {
            background-color: #FFD700;
            color: #000;
        }
</style>
<!-- Carrusel Bootstrap 5 -->
<div id="carouselSeries" class="carousel slide mb-5" data-bs-ride="carousel" data-bs-interval="4000">
    <div class="carousel-inner rounded shadow-lg">
        <div class="carousel-item active">
            <img src="https://cdn.hobbyconsolas.com/sites/navi.axelspringer.es/public/media/image/2022/11/stranger-things-4-2865541.jpg?tf=3840x.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Serie 1">
        </div>
        <div class="carousel-item">
            <img src="https://media.revistagq.com/photos/5cb72704773e5e65d3fdb3bf/16:9/w_1280,c_limit/series_netflix_2019_1049.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Serie 2">
        </div>
        <div class="carousel-item">
            <img src="https://cdn.computerhoy.com/sites/navi.axelspringer.es/public/media/image/2022/02/witcher-2620883.jpg?tf=3840x.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Serie 3">
        </div>
        <div class="carousel-item">
            <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQqI2Iyp5fsxJy6-sZCpDDIYWNPo5rGuhdT0w&s.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Serie 4">
        </div>
        <div class="carousel-item">
            <img src="https://i.blogs.es/63190b/dark-season3/1366_2000.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Serie 5">
        </div>
    </div>

    <!-- Controles opcionales -->
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselSeries" data-bs-slide="prev">
        <span class="carousel-control-prev-icon"></span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselSeries" data-bs-slide="next">
        <span class="carousel-control-next-icon"></span>
    </button>
</div>

<h1 class="mb-4">📺 Series por Género</h1>

<!-- Selector de género -->
<form method="get" asp-controller="Series" asp-action="Index" class="mb-4">
    <div class="row">
        <div class="col-md-4">
            <select name="idGenero" class="form-select" onchange="this.form.submit()">
                @foreach (var genero in Model.Generos)
                {
                    if (genero.Id == Model.GeneroSeleccionado)
                    {
                        <option value="@genero.Id" selected="selected">@genero.Nombre</option>
                    }
                    else
                    {
                        <option value="@genero.Id">@genero.Nombre</option>
                    }
                }
            </select>
        </div>
    </div>
</form>

<!-- Series -->
@if (Model.Peliculas != null && Model.Peliculas.Any())
{
    <div class="row">
        @foreach (var serie in Model.Peliculas)
        {
            <div class="col-md-3">
                <div class="card mb-4 shadow-sm h-100">
                    <img class="card-img-top" src="@serie.ImageURL" alt="Imagen no disponible" style="height: 300px; object-fit: cover;" />
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@serie.Title</h5>
                        <p class="card-text">@serie.Description?.Substring(0, Math.Min(100, serie.Description.Length))...</p>
                        <p><strong>Fecha:</strong> @(serie.ReleaseDate?.ToString("yyyy-MM-dd") ?? "No disponible")</p>
                        <p><strong>Calificación:</strong> ⭐ @(serie.Rating > 0 ? serie.Rating.ToString("0.0") : "No disponible")</p>

                        <!-- ⭐ Formulario para agregar a favoritos -->
                        <form asp-controller="Favoritos" asp-action="AgregarDesdeVista" method="post" class="mt-auto">
                            <input type="hidden" name="Id" value="@serie.Id" />
                            <input type="hidden" name="Title" value="@serie.Title" />
                            <input type="hidden" name="Description" value="@serie.Description" />
                            <input type="hidden" name="ImageURL" value="@serie.ImageURL" />
                            <input type="hidden" name="ReleaseDate" value="@serie.ReleaseDate?.ToString("yyyy-MM-dd")" />
                            <input type="hidden" name="Rating" value="@serie.Rating" />
                            <input type="hidden" name="MediaType" value="series" />
                            @if (Model.FavoritosIds.Contains(serie.Id))
                            {
                                <button class="btn btn-success mt-auto" disabled>
                                    ⭐ Agregado
                                </button>
                            }
                            else
                            {
                                <button type="button"
                                        class="btn btn-warning mt-auto"
                                        onclick="agregarAFavoritos(this, @serie.Id, 'series', @serie.Rating)">
                                    ⭐ Agregar a Favoritos
                                </button>
                            }
                        </form>
                        <form asp-controller="Watchlist" asp-action="Create" method="post">
                            <input type="hidden" name="UsuarioId" value="@Context.Session.GetInt32("UsuarioId")" />
                            <input type="hidden" name="ItemId" value="@serie.Id" />
                            <input type="hidden" name="Tipo" value="series" />
                            <input type="hidden" name="Prioridad" value="1" />
                            <button type="submit" class="btn btn-outline-success w-100 mt-2">➕ Agregar a Lista de Visualización</button>
                        </form>
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <p>⚠️ No se encontraron series para este género.</p>
}

@{
    var usuarioId = Context.Session.GetInt32("UsuarioId") ?? -1;
}

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            window.agregarAFavoritos = async function (button, itemId, tipo, rating) {
                const usuarioId = @usuarioId;

                if (usuarioId === -1) {
                    alert("Debe iniciar sesión para agregar a favoritos.");
                    return;
                }

                const favorito = {
                    usuarioId: usuarioId,
                    itemId: itemId,
                    tipo: tipo,
                    comentario: "",
                    calificacion: Math.max(1, Math.round(rating || 0))
                };

                const response = await fetch("https://localhost:7244/api/favoritos", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify(favorito)
                });

                if (response.ok) {
                    button.disabled = true;
                    button.classList.remove("btn-warning");
                    button.classList.add("btn-success");
                    button.innerHTML = "⭐ Agregado";
                } else {
                    alert("❌ Error al agregar a favoritos");
                }
            };
        });
    </script>
}









