@model PeliculasWeb.Models.HomeViewModel

<style>
    body {
        background-color: #111; /* Fondo general oscuro */
        color: #FFD700; /* Texto dorado */
    }

    .form-select {
        background-color: #000;
        color: #FFD700;
        border: 1px solid #FFD700;
    }

    .card {
        background-color: #000;
        border: 1px solid #FFD700;
        color: #FFD700;
    }

    .btn-warning {
        background-color: #FFD700;
        color: #000;
        border: none;
    }

        .btn-warning:hover {
            background-color: #e6c200;
        }

    .btn-outline-success,
    .btn-success {
        border-color: #FFD700;
        color: #FFD700;
        background-color: transparent;
    }

        .btn-outline-success:hover {
            background-color: #FFD700;
            color: #000;
        }

        .btn-success:disabled {
            background-color: #FFD700;
            color: #000;
        }
</style>

<!-- Carrusel Bootstrap 5 -->
<div id="carouselPeliculas" class="carousel slide mb-5" data-bs-ride="carousel" data-bs-interval="4000">
    <div class="carousel-inner rounded shadow-lg">
        <div class="carousel-item active">
            <img src="https://image.tmdb.org/t/p/original/qNBAXBIQlnOThrVvA6mA2B5ggV6.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Pelicula 1">
        </div>
        <div class="carousel-item">
            <img src="https://pymstatic.com/21157/conversions/peliculas-de-suspense-social.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Pelicula 2">
        </div>
        <div class="carousel-item">
            <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTzJ1Ozd9jLdWlkSyDf6O03pfsTvQUJGvHugw&s.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Pelicula 3">
        </div>
        <div class="carousel-item">
            <img src="https://image.tmdb.org/t/p/original/xDMIl84Qo5Tsu62c9DGWhmPI67A.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Pelicula 4">
        </div>
        <div class="carousel-item">
            <img src="https://image.tmdb.org/t/p/original/7bWxAsNPv9CXHOhZbJVlj2KxgfP.jpg" class="d-block w-100" style="height: 400px; object-fit: cover;" alt="Pelicula 5">
        </div>
    </div>

    <!-- Controles opcionales -->
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselPeliculas" data-bs-slide="prev">
        <span class="carousel-control-prev-icon"></span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselPeliculas" data-bs-slide="next">
        <span class="carousel-control-next-icon"></span>
    </button>
</div>


<h1 class="mb-4 text-center">🎬 Películas por Género</h1>

<!-- Selector de género -->
<form method="get" asp-controller="Peliculas" asp-action="Index" class="mb-4">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <select name="idGenero" class="form-select" onchange="this.form.submit()">
                @foreach (var genero in Model.Generos)
                {
                    if (genero.Id == Model.GeneroSeleccionado)
                    {
                        <option value="@genero.Id" selected="selected">@genero.Nombre</option>
                    }
                    else
                    {
                        <option value="@genero.Id">@genero.Nombre</option>
                    }
                }
            </select>
        </div>
    </div>
</form>

<!-- Películas -->
@if (Model.Peliculas != null && Model.Peliculas.Any())
{
    <div class="row">
        @foreach (var pelicula in Model.Peliculas)
        {
            <div class="col-md-3">
                <div class="card mb-4 shadow-sm h-100">
                    <img class="card-img-top" src="@pelicula.ImageURL" alt="Imagen no disponible"
                         style="height: 300px; object-fit: cover;" />
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@pelicula.Title</h5>
                        <p class="card-text">
                            @(pelicula.Description?.Length > 100
                                ? pelicula.Description.Substring(0, 100) + "..."
                                : pelicula.Description)
                        </p>
                        <p>ID: @pelicula.Id</p>
                        <p><strong>Fecha:</strong> @(pelicula.ReleaseDate?.ToString("yyyy-MM-dd") ?? "No disponible")</p>
                        <p><strong>Calificación:</strong> ⭐ @(pelicula.Rating > 0 ? pelicula.Rating.ToString("0.0") : "No disponible")</p>

                        @if (Model.FavoritosIds.Contains(pelicula.Id))
                        {
                            <button class="btn btn-success mt-auto" disabled>
                                ⭐ Agregado
                            </button>
                        }
                        else
                        {
                            <button class="btn btn-warning mt-auto"
                                    onclick="agregarAFavoritos(this, @pelicula.Id, 'movie', @Math.Max(1, Math.Round(pelicula.Rating)))">
                                ⭐ Agregar a Favoritos
                            </button>
                        }

                        @if (Model.WatchlistIds.Contains(pelicula.Id))
                        {
                            <button class="btn btn-success w-100 mt-2" disabled>
                                ✅ Agregado a Lista de Visualización
                            </button>
                        }
                        else
                        {
                            <form asp-controller="Watchlist" asp-action="Create" method="post">
                                <input type="hidden" name="ItemId" value="@pelicula.Id" />
                                <input type="hidden" name="UsuarioId" value="@Context.Session.GetInt32("UsuarioId")" />
                                <input type="hidden" name="Tipo" value="movie" />
                                <input type="hidden" name="Prioridad" value="1" />
                                <button type="submit" class="btn btn-outline-success w-100 mt-2">
                                    ➕ Agregar a Lista de Visualización
                                </button>
                            </form>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <p class="text-warning">⚠️ No se encontraron películas para este género.</p>
}

@{
    var usuarioId = Context.Session.GetInt32("UsuarioId") ?? -1;
}

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            window.agregarAFavoritos = async function (button, itemId, tipo, rating) {
                const usuarioId = @usuarioId;

                if (usuarioId === -1) {
                    alert("Debe iniciar sesión para agregar a favoritos.");
                    return;
                }

                const favorito = {
                    usuarioId: usuarioId,
                    itemId: itemId,
                    tipo: tipo,
                    comentario: "",
                    calificacion: Math.max(1, Math.round(rating))
                };

                const response = await fetch("https://localhost:7244/api/favoritos", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify(favorito)
                });

                if (response.ok) {
                    button.disabled = true;
                    button.classList.remove("btn-warning");
                    button.classList.add("btn-success");
                    button.innerHTML = "⭐ Agregado";
                } else {
                    alert("❌ Error al agregar a favoritos");
                }
            };
        });
    </script>
}






